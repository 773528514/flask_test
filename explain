上传文件

HTML
1、form属性设置：enctype属性设置为 'multipart / form-data'
2、从request.files[]对象中提取文件，并将其保存到所需的位置
    app.config['UPLOAD_FOLDER']	    定义上传文件夹的路径
    app.config['MAX_CONTENT_PATH']	指定要上传的文件的最大大小（以字节为单位）